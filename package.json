{
  "name": "my-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:api": "turbo run dev --filter=api",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.7.3"
  },
  "packageManager": "pnpm@9.4.2"
}
